---
description: Create a deep work plan (provided by the installed deepworkplan skill)
---

# /dwp-create

Route to `.agents/skills/deepworkplan/create/SKILL.md` and execute its flow.
Outputs land in `.dwp/plans/` and `.dwp/drafts/`.
